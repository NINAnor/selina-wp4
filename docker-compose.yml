services:
  app:
    build:
      context: .
    environment:
        DEBUG: True
    develop:
      watch:
        - action: sync
          path: .
          target: /app
          ignore:
            - .venv/
        - action: rebuild
          path: ./pyproject.toml
    ports:
      - 8000:8000
