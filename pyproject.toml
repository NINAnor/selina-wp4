
[project]
authors = [
  {name = "ninanor", email = "ninanor@nina.no"}
]
# PEP 621 project metadata
# See https://www.python.org/dev/peps/pep-0621/
dependencies = [
  "django-environ"
]
description = ""
license = "GPL-3.0+"
name = "selina_wp4"
readme = "README.md"
requires-python = ">=3.10"
version = "0.1.0"


[project.scripts]
selina_wp4 = "main:start"

[dependency-groups]
dev = [
  "deptry",
]

[tool.ruff]
fix = true

[tool.ruff.lint]
ignore = ["COM812"]
select = ["E", "W", "I", "F", "UP", "S", "B", "A", "COM", "LOG", "PTH", "Q"]
